enum SyntaxTheme {
  darcula("darcula"),
  monokai("monokai"),
  notepad("notepad"),
  matrix("matrix"),
  pastel("pastel"),
  atomOne("atomone");

  final String key;

  const SyntaxTheme(this.key);
}
